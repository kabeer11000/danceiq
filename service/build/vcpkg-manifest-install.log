Detecting compiler hash for triplet x64-osx...
Compiler found: /Library/Developer/CommandLineTools/usr/bin/c++
All requested packages are currently installed.
Total install time: 1.59 us
The package fmt provides CMake targets:

    find_package(fmt CONFIG REQUIRED)
    target_link_libraries(main PRIVATE fmt::fmt)

    # Or use the header-only version
    find_package(fmt CONFIG REQUIRED)
    target_link_libraries(main PRIVATE fmt::fmt-header-only)

opencv provides CMake targets:

  # this is heuristically generated, and may not be correct
  find_package(OpenCV CONFIG REQUIRED)
  # note: 12 additional targets are not displayed.
  target_link_libraries(main PRIVATE opencv_ml opencv_dnn opencv_core opencv_flann)

websocketpp provides CMake targets:

  # this is heuristically generated, and may not be correct
  find_package(websocketpp CONFIG REQUIRED)
  target_link_libraries(main PRIVATE websocketpp::websocketpp)

socket-io-client provides CMake targets:

  # this is heuristically generated, and may not be correct
  find_package(sioclient CONFIG REQUIRED)
  target_link_libraries(main PRIVATE sioclient::sioclient sioclient::sioclient_tls)

